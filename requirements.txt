typer>=0.12.3
rich>=13.7.1
PyYAML>=6.0.1
requests>=2.31.0

# Heavy AI worker deps — only needed if you use the AI classifier path.
# Install via `pip install -e ".[extractor]"` if you want them.
# PyMuPDF>=1.24.2
# python-docx>=1.1.2

# Distributed backend
# redis>=5.0.0

# Dev / test
pytest>=8.0.0
